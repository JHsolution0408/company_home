# dev.jh-solution.net 은 robots.txt 로 막지 않고 noindex 메타 태그로 색인을 막는다.
# 크롤링을 막으면 크롤러가 noindex 를 읽지 못해 URL 만 색인되는 경우가 생긴다.
# https://developers.google.com/search/docs/crawling-indexing/block-indexing
User-agent: *
Allow: /
